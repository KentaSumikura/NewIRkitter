<?xml version="1.0" encoding="utf-8"?>

<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ボタンが押されていない状態 -->
    <item android:state_pressed="false">
        <!-- shape:rectangle(=四角形) -->
        <shape android:shape="rectangle">

            <!-- 角の丸み -->
            <corners android:radius="8dp" />

            <!-- グラデーション -->
            <!-- angle:角度、type:linear(=線形) -->
            <gradient
                android:angle="90"
                android:endColor="#E26A6A"
                android:startColor="#E26A6A"
                android:type="linear" />

        </shape>
    </item>

    <!-- ボタンが押された状態 -->
    <item android:state_pressed="true">
        <!-- shape:rectangle(=四角形) -->
        <shape android:shape="rectangle">

            <!-- 角の丸み -->
            <corners android:radius="8dp" />

            <!-- グラデーション -->
            <!-- angle:角度、type:linear(=線形) -->
            <gradient
                android:angle="90"
                android:endColor="#D64541"
                android:startColor="#D64541"
                android:type="linear" />

        </shape>
    </item>

</selector>